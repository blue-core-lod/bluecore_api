import pytest

from bluecore.models.work import Work, WorkEmbedding, WorkVersion

def test_work(mocker):
    assert mocker

def test_work_single_embedding(mocker):
    assert mocker

def test_work_multiple_embeddings(mocker):
    assert mocker

def test_work_version(mocker):
    assert mocker
