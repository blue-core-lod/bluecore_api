import pytest

from bluecore.models.instance import Instance, InstanceEmbedding, InstanceVersion

def test_instance(mocker):
    assert mocker


def test_instance_single_embedding(mocker):
    assert mocker


def test_instance_multiple_embeddings(mocker):
    assert mocker


def test_instance_version(mocker):
    assert mocker
